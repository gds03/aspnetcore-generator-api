version: '3.1'

# CI Environments don't expose ports they communicate between them
services:
  generator:
    image: my-registry:55000/gen:ci-${GENERATOR_BUILD_NUMBER-1}
    #ports:
    #  - "8080:80"
  mail:
    image: mailhog/mailhog
    #ports:
    #  - "8025:8025"
  integration:
    image: my-registry:55000/gen:integration-${GENERATOR_BUILD_NUMBER-BUILD_NUMBER-1}
    build: . 
    environment: 
      - TEAMCITY_PROJECT_NAME